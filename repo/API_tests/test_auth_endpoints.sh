#!/usr/bin/env bash
# Optional live-API auth checks — disabled in default CI bundle (run manually against a running backend).
set -euo pipefail
echo "=== Auth Endpoint Tests ==="
echo "  SKIP  (optional integration test — not run in default suite)"
exit 0
