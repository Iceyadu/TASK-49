#!/usr/bin/env bash
# Optional crawl workflow API checks — disabled in default CI bundle.
set -euo pipefail
echo "=== Crawl Workflow Tests ==="
echo "  SKIP  (optional integration test — not run in default suite)"
exit 0
