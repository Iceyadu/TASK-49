#!/usr/bin/env bash
# Optional user-management API checks — disabled in default CI bundle.
set -euo pipefail
echo "=== User Management Tests ==="
echo "  SKIP  (optional integration test — not run in default suite)"
exit 0
